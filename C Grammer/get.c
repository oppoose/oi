#include <stdio.h>
#include <string.h>
#include "quickpower.h"
int main ()
{
   int str[50];
 
    char buf[100];
    fgets(buf, sizeof(buf), stdin);
   memset(str,0,sizeof(str));
   for(int i=0;i<6;i++){
       printf("%d ",st
   }
   
   return(0);
}